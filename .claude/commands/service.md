# 새 서비스 추가

프로젝트 컨벤션에 맞는 새 서비스 클래스를 생성합니다.

## 작업 순서

1. `docs/architecture.md`의 서비스 패턴을 확인합니다.
2. `.claude/templates/service.md` 템플릿을 참고하여 `src/services/{name}Service.js` 파일을 생성합니다.
3. 필요한 경우 `src/utils/utils.js`에 헬퍼 함수를 추가합니다.

## 서비스 정보

$ARGUMENTS
